// number
const a = 7; // literal concept

// const b = Number(9); // using the number constructor function

// operator

// special number - Infinity, -Infinity, NaN
// console.log(-2 / 'a');

// bigint
// const big = 10n;

// string
const name = 'temi';
const age = '27 years old';

const address = `No 1, iwo road,
qwerty
along osun road`;

const greetings = 'You are welcome';

console.log(`${greetings} ${name}`);
console.log(`${greetings} ${name}`);
console.log(`${name}'s Computer`);

const school = String('Lautech');

console.log(school);

// boolean
const isMale = true;

console.log(isMale);

// boolean values comes as a result of comparison operations

// null
const nothing = null;

// undefined
let messafe;

console.log(messafe);
// objects and symbols

// typeof operator

console.log(typeof message);

console.log();
